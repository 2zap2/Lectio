beautifulsoup4==4.12.3
lxml==5.3.0
python-dateutil==2.9.0.post0

# Optional: cookie refresh (--refresh-cookie flag, scripts/refresh_cookie.ps1)
# Install once with:
#   py -m pip install playwright
#   py -m playwright install chromium
# playwright>=1.40
